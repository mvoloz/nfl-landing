import React from 'react';

const displayName = 'NoFormerTeams';

const NoFormerTeams = ({ formerTeams }) => {
  return (
    <tr>
      <td>No Former Teams</td>
    </tr>
  );
};

NoFormerTeams.displayName = displayName;

export default NoFormerTeams;
